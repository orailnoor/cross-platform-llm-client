import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color bg = Color(0xFF0A0A0F);
  static const Color surface = Color(0xFF14141F);
  static const Color surfaceLight = Color(0xFF1E1E2E);
  static const Color card = Color(0xFF1A1A28);

  // Primary / Accent
  static const Color primary = Color(0xFF00D4AA);
  static const Color primaryDim = Color(0xFF00A885);
  static const Color secondary = Color(0xFF8B5CF6);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color textMuted = Color(0xFF6B7280);

  // Semantic
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Chat Bubbles
  static const Color userBubble = Color(0xFF1E3A5F);
  static const Color aiBubble = Color(0xFF1C1C2E);
  static const Color cmdBubble = Color(0xFF1A2E1A);

  // Border
  static const Color border = Color(0xFF2A2A3A);
  static const Color borderLight = Color(0xFF3A3A4A);
}
